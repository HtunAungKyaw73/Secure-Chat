generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Message {
  id        String   @id @default(cuid())
  text      String
  username  String
  createdAt DateTime @default(now())
}
